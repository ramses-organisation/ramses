name: On Push

on:
  push:
    branches:
      - dev
      - openmp
      - openmp_cicd
  workflow_dispatch:


jobs:
  build:
    name: Build Ramses
    uses: ./.github/workflows/build.yaml

  tests:
    name: Test Ramses
    uses: ./.github/workflows/tests.yaml
